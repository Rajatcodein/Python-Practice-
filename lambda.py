# Lambda functions has no name 
# lambda has no return value (infact return a functions)
# lambda is written in one line 
# not reusable 
# lambda are used as high order functions

a= lambda s: 'a' in s
print(a('hellow '))

n = lambda x: 'even' if x %2 ==0 else 'odd'
print(n(5))


